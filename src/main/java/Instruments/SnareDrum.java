package Instruments;

import BaseClasses.Instrument;

public class SnareDrum extends Instrument {
    public SnareDrum(String name, String sound, Double buyPrice, Double sellPrice, Enum instrumentType) {
        super(name, sound, buyPrice, sellPrice, instrumentType);
    }
}
